name: Invio Grafico Unificato ICON-CH1

on:
  schedule:
    # Esegue ogni ora al minuto 32 (8 run ensemble al giorno)
    - cron: '32 * * * *'
  
  workflow_dispatch:

jobs:
  genera_e_invia_iconch1:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installa dipendenze
        run: |
          python -m pip install --upgrade pip
          pip install requests numpy pandas matplotlib

      - name: Esegui Script ICON-CH1
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_THREAD_ID_HD: ${{ secrets.TELEGRAM_THREAD_ID_HD }}
        run: python icon_ch1_profile.py

      - name: Salva aggiornamento orario (Commit & Push)
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add ultima_ora_icon_ch1.txt || true
          git commit -m "Aggiornamento orario ICON-CH1 [skip ci]" || echo "Nessun nuovo run ICON-CH1"
          git pull --rebase origin main
          git push
